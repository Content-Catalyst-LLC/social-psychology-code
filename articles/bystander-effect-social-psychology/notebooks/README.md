# Notebooks

Generate the dataset first:

```bash
python3 ../python/bystander_effect_model.py --simulate --output ../data/bystander_effect_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook bystander_effect_workflow.ipynb
```
