# Notebooks

Generate the dataset first:

```bash
python3 ../python/groupthink_model.py --simulate --output ../data/groupthink_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook groupthink_workflow.ipynb
```
