# Notebooks

Generate the dataset first:

```bash
python3 ../python/cognitive_dissonance_model.py --simulate --output ../data/dissonance_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook cognitive_dissonance_workflow.ipynb
```
