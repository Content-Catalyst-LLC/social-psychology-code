# Notebooks

Generate the dataset first:

```bash
python3 ../python/intergroup_conflict_model.py --simulate --output ../data/intergroup_conflict_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook intergroup_conflict_workflow.ipynb
```
