# Notebooks

Generate the dataset first:

```bash
python3 ../python/obedience_authority_model.py --simulate --output ../data/obedience_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook obedience_authority_workflow.ipynb
```
