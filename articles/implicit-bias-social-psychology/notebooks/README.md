# Notebooks

Generate the dataset first:

```bash
python3 ../python/implicit_bias_model.py --simulate --output ../data/implicit_bias_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook implicit_bias_workflow.ipynb
```
